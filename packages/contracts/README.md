# `@litemcp/contracts`

Shared Zod schemas and inferred TypeScript types for organizations,
environments, MCP servers, tools, compositions, policy, identities, sessions,
approvals, audit events, and API envelopes.

This package defines wire and persistence shapes; it does not implement product
behavior. Prefer importing a schema here instead of repeating request validation
inside an app.
