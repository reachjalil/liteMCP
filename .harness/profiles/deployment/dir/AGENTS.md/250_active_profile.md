
## Active focus: deployment

Render and dry-run before mutation. Track one immutable revision through
configuration checks, migrations, deployment, public and authenticated smoke,
release evidence, and rollback planning. Keep secrets step-scoped and verify
repository-admin environment protection before production; a YAML environment
name alone is not protection. Use `litemcp-deployment-readiness`.
