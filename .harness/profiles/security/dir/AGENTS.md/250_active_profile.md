
## Active focus: security

Map the trust boundary and data classification before editing. Require explicit
tenant and actor provenance, least privilege, negative and cross-tenant tests,
revocation evidence, secret-safe output, and fail-closed behavior for
authorization and audit integrity. Use `litemcp-security-review` and do not turn
an unresolved high-severity advisory into an accepted release warning.
