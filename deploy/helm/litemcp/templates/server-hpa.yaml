{{- if .Values.autoscaling.server.enabled }}
{{- if lt (int .Values.autoscaling.server.maxReplicas) (int .Values.autoscaling.server.minReplicas) }}
{{- fail "autoscaling.server.maxReplicas must be greater than or equal to minReplicas" }}
{{- end }}
{{- if and (not .Values.autoscaling.server.targetCPUUtilizationPercentage) (not .Values.autoscaling.server.targetMemoryUtilizationPercentage) }}
{{- fail "autoscaling.server requires at least one CPU or memory utilization target" }}
{{- end }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "litemcp.server.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "litemcp.labels" . | nindent 4 }}
    app.kubernetes.io/component: server
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "litemcp.server.fullname" . }}
  minReplicas: {{ .Values.autoscaling.server.minReplicas }}
  maxReplicas: {{ .Values.autoscaling.server.maxReplicas }}
  metrics:
    {{- with .Values.autoscaling.server.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ . }}
    {{- end }}
    {{- with .Values.autoscaling.server.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ . }}
    {{- end }}
  {{- with .Values.autoscaling.server.behavior }}
  behavior:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
